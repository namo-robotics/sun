// stdlib/option.sun - Optional values and fallible results
//
// Option<T> represents a value that may be absent; Result<T, E> represents
// success or failure. Both are payload enums inspected with match:
//
//   var found = m.find(key);
//   var v = match found {
//       Option.Some(x) => x,
//       Option.None => fallback
//   };
//
// Type arguments are inferred from payload arguments (Option.Some(42)) or
// from the expected type (return position, annotations, field assignment).

/** Provides the standard-library declarations implemented in this source file. */
public module std {
  /** Contains an owned value when present, or records that no value is available. */
  public enum Option<T> {
    Some(T),
    None
  }

  /** Contains either a successful value or an error value. */
  public enum Result<T, E> {
    Ok(T),
    Err(E)
  }
}
