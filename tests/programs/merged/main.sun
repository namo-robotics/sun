// main.sun - Uses functions from both module files
using math;

/** Runs the regression scenario and returns its observable result to the test harness. */
function main() i32 {
  var sum = add(5, 3);
  var product = multiply(4, 2);
  return sum + product;
}
