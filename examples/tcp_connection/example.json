{
  "title": "TCP Connection",
  "order": 70,
  "summary": "A raw TCP client/server built on the standard library's `TcpListener` and `TcpStream`. The `listener` binds to `127.0.0.1:8080` and prints whatever it receives; the `talker` connects and sends a message. Run the listener first, then the talker in a second terminal.",
  "files": ["listener.sun", "talker.sun"],
  "run": "./listener   # terminal 1\n./talker     # terminal 2"
}
