{
  "title": "Classes",
  "order": 10,
  "summary": "Classes are stack-allocated value types with methods and an `init` constructor. Class values are passed by `ref` (Sun never copies them implicitly). This `Point` computes the Manhattan distance between two points.",
  "files": ["main.sun"],
  "run": "./main"
}
