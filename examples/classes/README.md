# Classes

Classes are stack-allocated value types with methods and an `init` constructor,
passed by `ref`. This `Point` computes the Manhattan distance between two points.

```bash
./build.sh
./main
```
